name: Terraform Test Deployment

on:
    push:
        branches:
            - main
        paths:
            - "src/terraform/**"
    pull_request:
        branches:
            - main
    workflow_dispatch:

permissions:
    contents: read
    id-token: write

env:
    TF_WORKING_DIR: ./src/terraform

jobs:
    Terraform:
        name: Terraform Test Deployment
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: ${{ env.TF_WORKING_DIR }}
        steps:
            - name: Configure AWS Credentials with Secrets Region
              uses: aws-actions/configure-aws-credentials@v4
              with: 
                role-to-assume: ${{ vars.AWS_ROLE_ARN }}
                aws-region: ${{ vars.AWS_REGION }}
            - name: Checkout Code
              uses: actions/checkout@v4
      
            - name: Setup Terraform
              uses: hashicorp/setup-terraform@v3
            - name: Terraform Format
              id: fmt
              run: |
                terraform fmt -check -recursive -diff
              continue-on-error: true
            - name: Terraform Init
              run: |
                terraform init 
            - name: Terraform Plan
              run: |
                terraform plan 
            - name: Terraform Apply
              run: |
                terraform apply -auto-approve
            - name: Get API Gateway URL
              id: get_url
              run: |
                echo "TELEGRAM_WEBHOOK_URL=$(terraform output -raw telegram_webhook_url)" >> $GITHUB_ENV


    Telegram_webhook:
        name: Update Telegram Webhook
        runs-on: ubuntu-latest
        needs: Terraform
        steps:
            - name: Install jq
              run: sudo apt-get update && sudo apt-get install -y jq
            - name: Set Telegram Webhook if Changed
              env:
                TELEGRAM_WEBHOOK_URL: ${{ env.TELEGRAM_WEBHOOK_URL }}
              run: |
                CURRENT_URL=$(curl -s "https://api.telegram.org/bot${{ secrets.TELEGRAM_API_KEY }}/getWebhookInfo" | jq -r '.result.url')
                echo "Current webhook URL: $CURRENT_URL"
                echo "New webhook URL: $TELEGRAM_WEBHOOK_URL"
                if [ "$CURRENT_URL" != "$TELEGRAM_WEBHOOK_URL" ]; then
                    echo "Webhook URL changed. Updating..."
                    curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_API_KEY }}/setWebhook" \
                        -d "url=${TELEGRAM_WEBHOOK_URL}"
                else
                    echo "Webhook URL is the same. No update needed."
                fi